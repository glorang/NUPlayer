<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="LeanbackPreferences" parent="Theme.Leanback">
        <item name="preferenceTheme">@style/PreferenceThemeOverlay.v14.Leanback</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:colorPrimary">@color/fastlane_background</item>
        <item name="android:windowAllowReturnTransitionOverlap">true</item>
        <item name="android:windowAllowEnterTransitionOverlap">true</item>
    </style>

    <!-- Use in Card Presenters -->
    <style name="BaseCardStyle" parent="Widget.Leanback.BaseCardViewStyle">
        <item name="cardType">mainOnly</item>
        <item name="cardBackground">@null</item>
    </style>

    <!-- This is supposed to hide the controls overlay after 3 seconds but doens't work unfortunately... -->
    <attr name="playbackControlsAutoHideTimeout" format="integer">3000</attr>
    <attr name="playbackControlsAutoHideTickleTimeout" format="integer">3000</attr>
</resources>